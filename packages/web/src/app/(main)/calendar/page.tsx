import {CalendarClient} from '@/components/features/calendar/calendar-client';

export default function CalendarPage() {
  return (
    <div className="px-0 sm:px-6 lg:px-8 py-6 max-w-[1400px] mx-auto">
      <CalendarClient />
    </div>
  );
}
